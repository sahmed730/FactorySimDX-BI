# Future Simulator Improvements

## Advanced Granular Disturbances (Phase 1.5)
- Allow custom disturbances per-machine, per-sensor (Temperature, Vibration, Pressure, etc.).
- Allow user to specify specific ranges for the anomaly: `Upper Limit`, `Lower Limit`.
- UI should allow selecting a machine, a sensor, and injecting the exact bounded range.
- `Mode` should toggle between `Ideal` and `Custom Disturbance` for highly precise ML labeling.

*Status: Parked for later discussion. Focus shifting to Phase 2 (Data Platform).*
